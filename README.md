# Angular 5 Dynamic Remote Module Loading

serve angular modules from a server and load them into angular dynamically.

## Install

```
cd server && yarn
cd client && yarn
```

## Run

```
cd server && yarn build && yarn start &
cd client && yarn build && yarn start &
```
